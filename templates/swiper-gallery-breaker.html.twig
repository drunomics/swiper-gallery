{% set classes = classes|default([])|merge(['gallery-slide swiper-slide swiper-slide-breaker swiper-slide-' ~ variant]) %}
{% set attributes = attributes|default(create_attribute()).addClass(classes) %}

<div{{ attributes }} data-hash="{{ id }}">
    <div class="gallery-breaker">{{ breaker }}</div>
</div>
